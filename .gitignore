node_modules/
dist/
.env
*.log
.DS_Store

# Real authoritative agent mapping (contains absolute paths and local secrets)
authoritative-agent-mapping.json

# Runtime broker configuration (per-machine, not in code audit scope)
broker/config.json
